# Python Web UI Automation

Projeto simples de **automação de interface web utilizando Python**, desenvolvido com foco em prática, aprendizado e organização de código.

Este projeto foi construído durante a **Jornada Python do canal Hashtag**, porém a implementação foi feita por mim, sem cópia direta de código, servindo como exercício prático para consolidar os conceitos apresentados.

---

## 🎯 Objetivo do Projeto

O objetivo deste projeto é demonstrar, de forma prática, como automatizar interações em uma interface web utilizando Python, simulando ações comuns realizadas por um usuário, como:

- acesso a páginas web;
- preenchimento de campos;
- cliques em botões;
- execução de fluxos automatizados.

---

## 🧠 Conceitos Praticados

Durante o desenvolvimento, foram aplicados e reforçados os seguintes conceitos:

- automação de interface web com Python;
- organização de um mini-projeto;
- separação de responsabilidades no código;
- leitura e entendimento de elementos da interface (UI);
- execução de scripts automatizados de forma controlada.

---

## 🛠️ Tecnologias Utilizadas

- **Python**
- Biblioteca de automação web (ex: Selenium / Playwright)
- Ambiente virtual (`venv`)
- Git e GitHub para versionamento

> *As bibliotecas utilizadas podem ser ajustadas conforme a necessidade do ambiente.*

---

## 📁 Estrutura do Projeto

```text
python-web-ui-automation/
├── src/
│   └── main.py
├── requirements.txt
└── README.md
```

Estrutura simples, focada em clareza e fácil entendimento do fluxo de execução.

## ▶️ Como Executar o Projeto

Clone o repositório:

```bash
git clone https://github.com/IcaroSantos21/python-web-ui-automation.git
```

Acesse o diretório do projeto:

```bash
cd python-web-ui-automation
```

Crie e ative um ambiente virtual:

```bash
python -m venv venv
source venv/bin/activate  # Linux / Mac
venv\Scripts\activate     # Windows
```

Instale as dependências:

```bash
pip install -r requirements.txt
```

Execute o script:

```bash
python src/main.py
```

## 📚 Aprendizados

Mesmo sendo um projeto simples, ele foi importante para:

entender melhor o funcionamento de automações web;

ganhar confiança na escrita de código para resolver problemas práticos;

reforçar boas práticas de organização;

transformar aprendizado teórico em algo executável.

## 🚀 Próximos Passos

melhorar tratamento de erros;

adicionar logs para acompanhamento da execução;

parametrizar entradas;

escrever testes automatizados para partes do fluxo.

👤 Autor

Desenvolvido por Ícaro Santos
🔗 GitHub: https://github.com/IcaroSantos21